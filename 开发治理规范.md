# chromatography_workstation — 开发治理规范（强制公约）

> **Qt 5.14.2 / C++17 / CMake / MinGW 64-bit** 色谱工作站（CDS）
> 本文件是所有 agent 的**强制约束**。进入本仓库的每个会话 / 子代理都会经 `CLAUDE.md` 加载它，**开工前必读**。

**四份文档分工**（各司其职，别混淆）：

| 文档 | 作用 |
|---|---|
| `CLAUDE.md` | 精简铁律入口（AI 自动加载） |
| `00_工程骨架与模块契约.md` | 静态规格（接口签名冻结、依赖方向） |
| `PROJECT.md` | 动态进度看板（做到哪了、下一步） |
| **本文件** | **治理公约（怎么协作 / 提交 / 合并 / 完成）** |

---

## 0. 仓库拓扑（worktree 一览）

仓库用 **git worktree** 做多分支物理隔离。每个 worktree 是完整代码副本，各自独立编译、独立 build 目录，互不污染。

| 分支 | worktree 路径 | 归属 | 角色 |
|---|---|---|---|
| **main** | `E:/My_project/QT/chromatography_workstation`（主 checkout） | 主控线程 | 集成基线 + 主程序壳 + 契约/文档开发地 |
| **dev-core_model** | `worktrees/core_model` | core_model 线程 | 领域模型（地基，最先合） |
| **dev-core_processing** | `worktrees/core_processing` | core_processing 线程 | 处理引擎 |
| **dev-acq** | `worktrees/acq` | acq 线程 | 采集（自研，无逆向参考） |
| **dev-io** | `worktrees/io` | io 线程 | 导入导出转换器 |
| **dev-report** | `worktrees/report` | report 线程 | 报告生成器 |
| **dev-ui** | `worktrees/ui` | ui 线程 | 界面层 |

> `worktrees/` 已被 `.gitignore` 忽略：它们只是"把分支拿出来干活的位置"，不是仓库内容；分支本体在 `git branch` 里，删目录不影响分支。

---

## 1. 文件归属矩阵（谁拥有谁）

模块源码在 worktree 内的 `project/chromatography_workstation/<模块>/` 路径下。

| 归属 | 名下文件 |
|---|---|
| **main-owned** | Qt 主工程根：`main.cpp`、`widget.cpp/h/ui`、`CMakeLists.txt`、`CMakeLists.txt.user`；`cmake/cdsw_module.cmake`；仓库根全部 `*.md`（契约 / PROJECT.md / CLAUDE.md / 本文件）；`docs/`；`.gitignore` |
| **dev-core_model** | `project/chromatography_workstation/core_model/{include,src,tests,CMakeLists.txt}` |
| **dev-core_processing** | `project/chromatography_workstation/core_processing/**` |
| **dev-acq** | `project/chromatography_workstation/acq/**` |
| **dev-io** | `project/chromatography_workstation/io/**` |
| **dev-report** | `project/chromatography_workstation/report/**` |
| **dev-ui** | `project/chromatography_workstation/ui/**` |

**红线文件（main-owned 中最高危，模块 worktree 一律只读）：**
- `project/chromatography_workstation/CMakeLists.txt` — 唯一 master build，`add_subdirectory` 6 模块 + 主程序壳
- `project/chromatography_workstation/cmake/cdsw_module.cmake` — 统一建库约定
- `00_工程骨架与模块契约.md` §4 — 接口签名，冻结
- 已合入 main 的其他模块的 `include/<模块>/` 接口头 — 只能读、不能改

---

## 2. 开发路线判断（任务到手后按序执行）

**路线 A — 模块开发：在对应 worktree 里做。**
core_model → `worktrees/core_model`、core_processing → `worktrees/core_processing`、acq → `worktrees/acq`、io → `worktrees/io`、report → `worktrees/report`、ui → `worktrees/ui`。

**路线 B — 壳层 / 集成 / 文档：直接在主 checkout（main）里改。**
主程序壳（`main.cpp` / `widget.*`）、根 `CMakeLists.txt`、`cdsw_module.cmake`、所有 `*.md`、`docs/`。不占模块分支，直接在 main 开发、提交。

> **main 不是"只读成品仓库"，是活跃开发分支**：主程序壳、契约、文档的日常开发就在 main 上发生。只是模块功能不在 main 直接写，必须走模块分支。

### 判断步骤

1. 列出任务要改动的**全部文件**。
2. 全部落在某模块名下 → **路线 A**，进对应 worktree。
3. 全部落在 main 名下 → **路线 B**，直接在主 checkout 改。
4. **混合任务**（模块文件 + main 文件都要动）→ 拆分：
   - 模块部分 → 模块 worktree；
   - main 接线 / 契约部分 → **报给主控线程在 main 做**（模块线程绝不直接在 worktree 里改 main-owned 文件）。

---

## 3. 合并纪律与方向

**合并只有两个箭头，方向不可逆：**

```
         定期同步（把 main 拉进 dev 分支，防跑偏）
         ◄──────────────────────────────────────
         dev-<模块>  ───────────────────────────►  main
         功能完成 + ctest 全绿后（由主控合回）
```

1. **main → dev（定期同步）**：dev 分支定期 `git merge main`，别跑偏超过 1~2 轮。在模块 worktree 内执行。
2. **dev → main（合回）**：功能完成 + 全部验证通过后执行。**此动作永远在主 checkout（主控线程）执行**，按依赖顺序。
3. **合并门禁（缺一不可）**：编译通过 + 本模块 ctest 全绿 + 不影响已合并模块。
4. 冲突若出现在 main-owned 文件 → 由主控线程统一裁决，模块线程不得自行改 main-owned 文件来"解决冲突"。

**合并顺序（依赖方向，不可违反）**：

```
core_model → core_processing → acq → io → report → ui → 主工程集成
```

core_model 必须先合 main（所有模块依赖其头文件）；其后 core_processing / acq / io / report 可并行合；ui 最后。

---

## 4. 开发流程

### 路线 A — 模块任务完整流程

```bash
cd E:/My_project/QT/chromatography_workstation/worktrees/<模块>
git merge main                    # 1. 先同步 main 最新
# 2. 只改自己名下文件（§1）
# 3. 编译 + ctest 全绿
git add <名单> && git commit      # 4. 提交到 dev 分支（§5 规范）
# 5. 功能完成 + 全绿后，停下，等主控线程合回 main（不自己 merge main）
```

### 路线 B — 壳层 / 文档任务完整流程

```bash
cd E:/My_project/QT/chromatography_workstation   # 主 checkout
git add <名单> && git commit                     # 直接改、直接提交到 main
git push                                        # main 直接推远程
```

---

## 5. 提交规范

1. **每个 worktree / 分支改动独立提交**，不跨 worktree 混提。
2. **提交格式**：`type(模块): 中文摘要`，type ∈ `feat` / `fix` / `test` / `doc` / `refactor` / `chore`；正文用 `- ` 列表写"改了什么 / 为什么"。
3. **提交时机**：做到"可编译 / 可运行 / 测试绿"的稳定点就提交；每次变更会话结束提交本次改动，不留脏工作区。
4. **不提交构建产物**：`build*/` 已被 `.gitignore` 排除，不 `git add`。
5. **文档同步**：文档改动和代码改动一起 commit，不拆两处。

---

## 6. 记忆文件与错误上报（强制，每线程结束 / 卡住时）

### 6.1 记忆文件（每线程结束 / 中断前写）

每个 AI 线程结束或中断前，在 `docs/memory/<模块>.md` 追加一条（简短几行）：

```markdown
## <日期> <线程>
- 做了什么：
- 为什么这么设计：
- 下一步：
- 卡点/依赖：
```

> 这是**跨线程共享的开发记忆**：下一个续作的 AI 先读它 + `PROJECT.md` + `git status` 未提交 diff，就能无痛续上，不重复踩坑。

### 6.2 错误上报（解决不了时，等人工审核）

解决不了的错误 → 写 `docs/blockers/<模块>-<日期>.md`，固定四节：

```markdown
# <模块> — <一句话现象>（待人工审核）
## ① 现象        （错误信息 / 复现步骤）
## ② 已尝试      （已试过的方案及结果）
## ③ 卡点        （卡在哪、为什么过不去）
## ④ 需要的帮助  （需要人工做的决策 / 提供的线索）
```

**上报后线程停**：不空转、不硬猜、不改契约绕过。人工在文件里回复决策 → 线程读回复继续 → 解决后标"已解决"。

### 6.3 必须停下等人工的情况

改契约签名 / 遇到设计分叉 / 解决不了的错误 / 发现上游逆向文档矛盾 / 涉及硬件或危险操作。

---

## 7. 缺陷（Bug）修复流程

1. 发现 bug → 在 main 上记入 `docs/debt.md`（或 PROJECT.md 决策日志）。
2. 从 main 切 `hotfix/<简述>` 分支修复（若 bug 只在某模块，先在对应 dev 分支修再合回）。
3. 编译 + ctest 通过后，hotfix 合回 main。
4. 修复提交号同步记入文档。

---

## 8. 职责边界速查

| 动作 | 执行者 |
|---|---|
| 改模块文件、dev 分支提交 | 对应模块线程 |
| 改 main.cpp / widget.* / 根 CMake / 契约 / 文档 | 主控线程（main） |
| dev 分支定期 merge main | 对应模块线程 |
| dev 分支合回 main + 集成验证 | 主控线程（主 checkout） |
| 冲突裁决 | 主控线程 |
| 改接口签名 / 契约 §4 变更 | 主控线程（模块线程先申请） |
| 记忆文件 / blocker / PROJECT.md 更新 | 模块线程写，主控线程审 |

---

## 9. 工作方式（L0 / L1 / L2 分级，精简省 token）

本项目**不要求所有任务强制使用 Superpowers**。开始前先按复杂度判断工作方式：

### L0：直接执行
适用：简单 Bug 修复、编译错误、单函数修改、简单 UI 调整、变量重命名、小范围代码修改、明确且低风险的行为修改。
→ 直接分析、修改并验证，无需调用 Superpowers。

### L1：自主规划
适用：中等规模功能、涉及多文件修改、模块内部重构、已有模块的扩展。
→ 先分析现有代码和依赖，自主制定实施计划，再执行和验证。不强制 Superpowers。

### L2：Superpowers 工作流
适用：大型新功能、跨模块架构修改、大规模重构、长时间自主开发、需求明显不确定、需多 Agent 协同、可能影响大量现有功能。
此时用相关技能：
- 新功能 / 行为改动 → `brainstorming` → `writing-plans`
- Bug 修复且原因不明 → `systematic-debugging`
- 复杂代码实现 → `test-driven-development`
- 多任务计划执行 → `subagent-driven-development` 或 `dispatching-parallel-agents`
- 代码完成 → 编译 + `ctest` 全绿（本工程暂无静态检查脚本 / CI，用 cmake + ctest 代替）
- 验证通过后 → `requesting-code-review`（可选用，人力有限时）
- 收到 Review 意见 → `receiving-code-review`
- 宣称完成前 → `verification-before-completion`

### 验证要求（无论 L0 / L1 / L2 都要）

完成任务前必须**适当验证**，按任务类型选：编译、单元测试、`ctest`、手动功能验证。不要求为走形式而执行无关验证。

### 工作原则

1. 优先用模型自身分析 / 规划 / 实现 / 验证能力。
2. Superpowers 是复杂任务辅助，不是所有任务强制流程。
3. 不为调用 Skill 而调用 Skill。
4. 小任务直接完成，避免不必要 token 消耗。
5. 复杂任务用结构化工作流，降低长任务偏离 / 遗漏 / 回归风险。
6. 任务边界明确时不重复询问已明确信息。
7. 用户明确要求跳过 Superpowers → 直接执行，但仍要合理验证。

**优先级：本文件的项目约束 > 用户明确要求 > Superpowers 技能 > Agent 默认行为。**

---

## 10. 完成定义（DoD，合并前强制）

| # | 检查项 | 工具 | 违背后果 |
|---|---|---|---|
| D1 | 全量编译通过 | `cmake --build build` | 不合并 |
| D2 | 全部测试通过 | `ctest --test-dir build --output-on-failure` | 不合并 |
| D3 | 新逻辑有测试并 `add_test()` 注册 | `ctest -N` 核对 | 视为未完成 |
| D4 | 接口纪律：未 include 其他模块 `src/`、core 系未用 QtWidgets | 人工 / 查 | 返工 |
| D5 | 提交到对应分支 + push | `git push` | 视为未完成 |
| D6 | 更新 PROJECT.md + 写记忆文件 | 文档 | 视为未完成 |

> 主控线程合并时以此表为准；机器能查的（D1 / D2 / D3）一律以机器结果为准，不采信口头"测过了"。

---

## 11. 禁止事项速查（红线）

1. 不 `push --force`；不提交 `build/`、`worktrees/`、二进制、密码、密钥。
2. 模块线程不碰 main-owned 文件（§1 红线），不跨模块改文件。
3. 不改契约 §4 接口签名（改 = 先申请，主控登记）。
4. 不 include 其他模块的 `src/`；core 系模块不用 QtWidgets。
5. 不吞错误、不空 catch、不硬编魔法数（参数命名常量 + 注释来源）。
6. 解决不了的错误不硬猜、不空转 → 上报 blocker（§6.2）。
7. 不因测试麻烦就删测试或用 `QSKIP` 绕过。
8. 不自行 `git merge main` 合回（合回是主控线程动作）；模块线程只做"定期 merge main 同步"。
