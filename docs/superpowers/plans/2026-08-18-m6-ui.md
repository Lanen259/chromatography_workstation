# M6 ui —— 实现计划（2026-08-18 整夜自主开发）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 dev-ui 实现 §4.6 界面层（ChromatogramView / PeakTableView+Model / MethodEditorView / MainWindow / SelectionController），offscreen 冒烟全绿，合回 main。

**Architecture:** 全部 `namespace cdsw`；复用页面用 `.ui`（MainWindow.ui / MethodEditorView.ui），自绘控件（ChromatogramView）与表控件（PeakTableView）以 promote 占位进 MainWindow.ui；SelectionController 契约签名 + 增补 setChromatogram/setMethod；两阶段：Phase A 无 io（io 未合），Phase B 在 M4 合入后补 io 导入。

**Tech Stack:** Qt 5.14.2 MinGW、C++17、QtWidgets（ui 唯一）、Qt Test、QT_QPA_PLATFORM=offscreen、CMake AUTOUIC。

## Global Constraints
- **改动范围**：只许改 `project/chromatography_workstation/ui/` 下的 `include/ src/ tests/ CMakeLists.txt`；外加授权文档（设计/计划 spec、PROJECT.md §2、docs/memory/ui.md）。禁碰其他模块、禁碰 worktrees/acq。
- **契约 §4.6 冻结**：`SelectionController(Selection*, ProcessingPipeline*, QObject*)` / `onChromatogramChanged()` / `sigPeaksUpdated(const QList<Peak>&)` 逐字；可增补 setter（open-closed）。
- **风格 §8**：复用页面一律 `.ui`；禁 `setGeometry`、禁手动 new 拼布局；自绘控件 promote。
- **接口纪律**：视图只 include core 接口头 + io/report 接口头；ui 是唯一 QtWidgets 模块。
- **依赖**：Phase A 不依赖 io 接口头（io 未合 main），MainWindow 用 report 的 ReportRegistry（已合）。
- **提交格式**：`feat/ui:` / `test/ui:` / `doc/ui:` + 中文摘要。

## 构建命令
```bash
export PATH="/d/Program_flies/qt_creat/APP/5.14.2/mingw73_64/bin:/d/Program_flies/qt_creat/APP/Tools/mingw730_64/bin:$PATH"
cd /e/My_project/QT/chromatography_workstation/worktrees/ui
cmake -S project/chromatography_workstation -B build -DCDSW_BUILD_TESTS=ON -G "MinGW Makefiles" \
  -DCMAKE_CXX_COMPILER=D:/Program_flies/qt_creat/APP/Tools/mingw730_64/bin/g++.exe \
  -DCMAKE_C_COMPILER=D:/Program_flies/qt_creat/APP/Tools/mingw730_64/bin/gcc.exe \
  -DCMAKE_MAKE_PROGRAM=D:/Program_flies/qt_creat/APP/Tools/mingw730_64/bin/mingw32-make.exe \
  -DCMAKE_PREFIX_PATH=D:/Program_flies/qt_creat/APP/5.14.2/mingw73_64
cmake --build build --target ui ui_tests
QT_QPA_PLATFORM=offscreen ctest --test-dir build --output-on-failure -R ui
```

## 文件结构
| 文件 | 职责 |
|---|---|
| `include/ui/SelectionController.h` | 契约 §4.6 桥接（冻结签名 + setChromatogram/setMethod） |
| `include/ui/PeakTableModel.h` | `QAbstractTableModel` 适配 `QList<Peak>` |
| `include/ui/PeakTableView.h` | `QTableView` 子类，`setPeaks` |
| `include/ui/ChromatogramView.h` | 自绘曲线控件（缩放/平移/选区） |
| `include/ui/MethodEditorView.h` | Method.steps 增删改面板 |
| `src/MethodEditorView.ui` | 面板布局（列表+按钮+参数表） |
| `include/ui/MainWindow.h` + `src/MainWindow.ui` | 装配菜单/工具栏/分栏 |
| `src/*.cpp` | 各实现 |
| `tests/t_ui.cpp` | offscreen 冒烟 |
| `ui/CMakeLists.txt` | cdsw_add_module + .ui target_sources + offscreen ENV |

## Task 拆分（TDD 红→绿→提交）

### Task 1: SelectionController + 冒烟（真实管线峰检测）
- `include/ui/SelectionController.h`：契约签名 + `setChromatogram(Chromatogram*)` + `setMethod(const Method*)`。
- `tests/t_ui.cpp` 首用例 `selectionControllerRunsPipeline`：真实 `ProcessingPipeline(Registry::instance())` + Method(`first_derivative_peak_detector`, threshold=MEDIUM, windowSize=0) + 双高斯峰 Chromatogram（照 core_processing `makeTwoPeakChrom`）→ setChromatogram/setMethod → `onChromatogramChanged()` → QSignalSpy 收 `sigPeaksUpdated` 且 peaks.size()==2。
- 实现 `src/SelectionController.cpp`：`onChromatogramChanged` 里 chrom/method 非空才执行；setDirty(true) → execute → emit sigPeaksUpdated(pipeline->peaks())。
- 绿 → 提交 `feat(ui): SelectionController 桥接 + 真实管线冒烟`。

### Task 2: PeakTableModel + PeakTableView + 冒烟
- `PeakTableModel`：列 Peak # / Apex RT (min) / Start RT (min) / Stop RT (min) / Height / Area；`setPeaks(const QList<Peak>&)`；data() 用 `'f',6` 分钟 RT（照 report 峰表列语义）；rowCount/columnCount/headerData。
- `PeakTableView : QTableView`：持有 model 成员，`setPeaks` 转发。
- 冒烟：固定 2 峰 → rowCount==2、data(0,0)=="1"、data(0,1)=="1.250000"；空 → 0 行。
- 提交 `feat(ui): 峰表模型与视图`。

### Task 3: ChromatogramView（自绘曲线 + 缩放/平移/选区）+ 冒烟
- paintEvent 画折线（processedPoints 优先否则 signalPoints）+ 峰标记 + 选区阴影；wheel 缩放 X 轴、左键拖拽平移、shift+拖拽出选区；`sigSelectionRangeChanged(qint64 startMs, qint64 stopMs)`。
- `setChromatogram(Chromatogram*)`、`setPeaks(const QList<Peak>&)`、`setSelectionRange(qint64,qint64)`（外部写回）。
- 冒烟：setChromatogram 后 `repaint()` 不崩；模拟 wheel/drag 事件 → spy 收到信号且范围合理。
- 提交 `feat(ui): 色谱曲线视图（缩放/平移/选区）`。

### Task 4: MethodEditorView（.ui 装配）+ 冒烟
- `src/MethodEditorView.ui`：QWidget，QListWidget(steps) + 按钮行(Add/Remove/MoveUp/MoveDown) + QTableWidget(参数 k/v)。
- `setMethod(Method*)`（建列表）；`method()`；`sigMethodChanged()`；Add 用 `Registry::instance()` 的 availableFilterIds/availableBaselineIds/availablePeakDetectorIds/availableIntegratorIds/availableQuantifierIds 合并（QComboBox 选 id）。
- 冒烟：setMethod(2 steps) → 列表 2 项；Add("sg_smooth") → method steps 3；Remove/MoveUp 顺序正确；编辑参数写回 QVariantMap；sigMethodChanged 触发。
- 提交 `feat(ui): 方法编辑器（.ui 装配 + 注册表步骤）`。

### Task 5: MainWindow（.ui 装配，Phase A 无 io）+ 冒烟
- `src/MainWindow.ui`：QMainWindow，menuBar(File: Run Method/Export CSV Report/Quit；Help: About) + toolbar + central QSplitter(左 ChromatogramView 横、右 QSplitter 纵 PeakTableView+MethodEditorView)；ChromatogramView/PeakTableView promote。
- `MainWindow`：持有 Chromatogram/Method/Selection/ProcessingPipeline/SelectionController；`setChromatogram` 分发到视图+controller；Run Method → controller.onChromatogramChanged；Export CSV Report → ReportRegistry::reporterFor("CSV") 组装 ReportData 生成到路径。
- 冒烟（Phase A）：offscreen 创建不崩、菜单存在、setChromatogram 后视图不崩、Run Method 后峰表有数据。
- 提交 `feat(ui): MainWindow 装配（菜单/分栏/管线握手）`。

### Task 6: Phase B —— io 合入后补 MainWindow Import 菜单
- `#include <io/converters.h>`：Import 菜单 → QFileDialog 选 .csv → `ConverterRegistry::instance().importerFor(path)` → import → setChromatogram。
- 冒烟：构造临时 .csv（ExporterCsv 或手写）→ import → 色谱显示。
- 提交 `feat(ui): MainWindow 导入菜单（io 集成）`。

### Task 7: 全量验证 + 自审 + 合回
- dev-ui `cmake --build build` 0 error + 全套 ctest 绿（含 acq/report）；`git merge main` 后复跑。
- 独立审查子 agent 审 diff（Critical 全修）→ 合回 main → 主 checkout 全量构建 + 全套 ctest → PROJECT.md §2 ui 行更新 → 写 docs/memory/ui.md。

## Self-Review
- Spec 覆盖：§4.6 五个组件全部落 Task 1–5；io 依赖 Task 6；offscreen 冒烟 Task 1–5；DoD D1–D4 落 Task 7。
- 无占位符：每 Task 关键签名与测试断言已给。
- 类型一致：`setChromatogram(Chromatogram*)`、`setPeaks(QList<Peak>)`、`sigPeaksUpdated(QList<Peak>)`、`pipeline->execute(const Method&, Chromatogram&)` 全链一致。
