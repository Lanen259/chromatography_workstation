# M5 report 报告生成器 —— 实现计划（2026-08-18）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 dev-report 分支实现契约 §4.5 的 report 模块：`ReportData/IReporter/ReportRegistry` + ReporterCsv 分区式 CSV 报告器 + 金样逐字节比对测试，ctest 全绿。

**Architecture:** 接口头 `include/report/reporters.h` 逐字照契约；实现类 `ReporterCsv` 放 `src/`（私有，测试只走公开 API）；`ReportRegistry` 单例沿用 core_processing `Registry` 的「QHash 工厂表 + 探针取键 + 首调注册内置项」模式。CSV 输出三个节（表头节 + 峰表 + 定量表），LF 行尾、UTF-8、`'f',6` 定点、RT 分钟制，保证跨平台字节一致。

**Tech Stack:** Qt 5.14.2 MinGW 64-bit、C++17、CMake（MinGW Makefiles）、Qt Test、ctest。

## Global Constraints

- **改动范围**：只许改 `project/chromatography_workstation/report/` 下的 `include/ src/ tests/ CMakeLists.txt`；外加主控授权的三份文档：`docs/superpowers/specs/2026-08-18-report-design.md`（已提交）、`PROJECT.md §2`、`docs/memory/report.md`。禁止碰其他模块文件与 main-owned 红线文件。
- **契约 §4.5 冻结**：`ReportData / IReporter / ReportRegistry` 三个公开类型的公开签名逐字照契约，禁止改动。只许实现接口 / 新增实现类并注册（开闭原则）。
- **接口纪律**：只 include `core_model/include/` 与 `core_processing/include/` 的接口头；禁止 include 别家 `src/`。reporters.h 只引 QtCore。
- **纯 QtCore**：report 是 core 系模块，禁止 `QtWidgets`/`QtGui`/`QtQml`。
- **行尾**：CSV 一律 LF（QFile 不带 `QIODevice::Text` 打开）；`report/tests/data/.gitattributes` 写 `*.csv text eol=lf` 防 autocrlf 在检出时转 CRLF。
- **金样纪律**：`tests/data/golden_report.csv` 手写推导，绝不从生成器输出回抄（防「金样由同一代码生成」的循环验证）。
- **提交格式**：`feat(report):` / `test(report):` / `doc(report):` + 中文摘要；构建产物不提交。
- **工具链**：Qt `D:/Program_flies/qt_creat/APP/5.14.2/mingw73_64`，MinGW `D:/Program_flies/qt_creat/APP/Tools/mingw730_64/bin`，生成器 `MinGW Makefiles`。每次构建/测试前先 export PATH。

## 文件结构

| 文件 | 职责 |
|---|---|
| `report/include/report/reporters.h` | 契约 §4.5 三个公开类型（唯一公开接口，冻结） |
| `report/src/ReporterCsv.h` | `ReporterCsv : IReporter` 声明（模块内私有） |
| `report/src/ReporterCsv.cpp` | CSV 分区式写入 + RFC4180 引号（私有） |
| `report/src/ReportRegistry.cpp` | 单例 + 内置注册（探针取 formatName 作键） |
| `report/tests/t_report.cpp` | QTest：注册表/金样/边界/引号/失败路径 |
| `report/tests/data/golden_report.csv` | 金样参考文本（手写，LF） |
| `report/tests/data/.gitattributes` | 强制 golden 为 LF |
| `report/CMakeLists.txt` | 追加测试编译宏 `REPORT_GOLDEN_DIR` |

## 通用构建命令（每步验证用）

```bash
export PATH="/d/Program_flies/qt_creat/APP/5.14.2/mingw73_64/bin:/d/Program_flies/qt_creat/APP/Tools/mingw730_64/bin:$PATH"
# 首次配置（build/ 目录在 report worktree 根下）
cmake -S project/chromatography_workstation -B build -DCDSW_BUILD_TESTS=ON -G "MinGW Makefiles" \
      -DCMAKE_PREFIX_PATH="D:/Program_flies/qt_creat/APP/5.14.2/mingw73_64"
# 构建 report 库 + 测试
cmake --build build --target report report_tests
# 跑测试（先有 PATH 才有 Qt DLL，否则 0xc0000135）
ctest --test-dir build --output-on-failure -R report
```

---

### Task 1: 契约接口头 + ReporterCsv 桩 + 注册表分发（TDD 红→绿）

**Files:**
- Create: `project/chromatography_workstation/report/include/report/reporters.h`
- Create: `project/chromatography_workstation/report/src/ReporterCsv.h`
- Create: `project/chromatography_workstation/report/src/ReporterCsv.cpp`（generate 桩：返回 false）
- Create: `project/chromatography_workstation/report/src/ReportRegistry.cpp`（完整，含内置注册 CSV）
- Create: `project/chromatography_workstation/report/tests/t_report.cpp`（仅 `registryDispatch` 用例）
- Create: `project/chromatography_workstation/report/tests/data/.gitattributes`
- Modify: `project/chromatography_workstation/report/CMakeLists.txt`

**Interfaces:**
- Consumes: 契约 §4.5 冻结签名；`cdsw::Peak`（core_model）、`cdsw::QuantEntry`（core_processing）。
- Produces: `cdsw::ReportRegistry::instance()`、`registerReporter(IReporter* (*)())`、`availableFormats()`、`reporterFor(const QString&)`；`cdsw::IReporter::{formatName,fileSuffix,generate}`。

- [ ] **Step 1: 写失败测试（registryDispatch）**

`tests/t_report.cpp`：

```cpp
// report/tests/t_report.cpp —— 报告器 QTest（契约 §4.5 + 金样逐字节比对）
#include <QtTest/QtTest>

#include <QDateTime>
#include <QFile>
#include <QFileInfo>
#include <QTemporaryDir>

#include <memory>

#include <report/reporters.h>

using cdsw::ReportData;
using cdsw::IReporter;
using cdsw::ReportRegistry;
using cdsw::Peak;
using cdsw::QuantEntry;

class ReportTest : public QObject
{
    Q_OBJECT

private slots:
    void registryDispatch();
    void goldenReportMatchesReference();
    void emptyDataBoundary();
    void peaksWithoutQuant();
    void rfc4180Quoting();
    void generateFailsOnBadPath();

private:
    static ReportData makeGoldenData();
    static QByteArray readFile(const QString& path);
};

// —— 金样固定数据（数值取干净分钟值，便于人工复核）——
ReportData ReportTest::makeGoldenData()
{
    ReportData d;
    d.sampleName = QStringLiteral("Benzene Calibration Std");
    d.methodName = QStringLiteral("External Standard");
    d.acquiredAt = QDateTime(QDate(2026, 8, 18), QTime(10, 30, 0)); // Qt::LocalTime 默认 → ISODate 无时区后缀
    // 峰故意乱序传入：输出必须按 apexRTMs 升序重排后编号
    d.peaks = {
        Peak{ 186000, 204000, 222000, 31000.0, 77500.0 },   // apex 3.4 min
        Peak{ 60000,  75000,  90000,  52000.0, 104000.0 },  // apex 1.25 min
    };
    d.quantEntries = {
        QuantEntry{ 75000, QStringLiteral("Benzene"), 104000.0, 12.5, QStringLiteral("mg/L") },
        QuantEntry{ 204000, QStringLiteral("Toluene"), 77500.0, 25.0, QStringLiteral("mg/L") },
    };
    return d;
}

QByteArray ReportTest::readFile(const QString& path)
{
    QFile f(path);
    if (!f.open(QIODevice::ReadOnly))
        return QByteArray();
    return f.readAll();
}

void ReportTest::registryDispatch()
{
    auto& registry = ReportRegistry::instance();

    // 内置 CSV 已注册
    QVERIFY(registry.availableFormats().contains(QStringLiteral("CSV")));

    std::unique_ptr<IReporter> csv(registry.reporterFor(QStringLiteral("CSV")));
    QVERIFY(csv);
    QCOMPARE(csv->formatName(), QStringLiteral("CSV"));
    QCOMPARE(csv->fileSuffix(), QStringLiteral(".csv"));

    // 未注册格式 → nullptr
    QCOMPARE(registry.reporterFor(QStringLiteral("PDF")), nullptr);

    // 开放注册：新格式 = 注册一行（开闭原则）
    struct FakeReporter : IReporter {
        QString formatName() const override { return QStringLiteral("FAKE"); }
        QString fileSuffix() const override { return QStringLiteral(".fake"); }
        bool generate(const ReportData&, const QString&) override { return true; }
    };
    registry.registerReporter([]() -> IReporter* { return new FakeReporter; });
    QVERIFY(registry.availableFormats().contains(QStringLiteral("FAKE")));
    std::unique_ptr<IReporter> fake(registry.reporterFor(QStringLiteral("FAKE")));
    QVERIFY(fake);
}

// —— 以下用例在 Task 2 实现 generate 后补全，本任务先以 stub 让编译通过 ——
void ReportTest::goldenReportMatchesReference() { QSKIP("Task 2 实现"); }
void ReportTest::emptyDataBoundary()            { QSKIP("Task 2 实现"); }
void ReportTest::peaksWithoutQuant()            { QSKIP("Task 2 实现"); }
void ReportTest::rfc4180Quoting()               { QSKIP("Task 2 实现"); }
void ReportTest::generateFailsOnBadPath()       { QSKIP("Task 2 实现"); }

QTEST_APPLESS_MAIN(ReportTest)
#include "t_report.moc"
```

`tests/data/.gitattributes`：

```
*.csv text eol=lf
```

- [ ] **Step 2: 跑构建，确认失败**

Run:
```bash
cmake -S project/chromatography_workstation -B build -DCDSW_BUILD_TESTS=ON -G "MinGW Makefiles" -DCMAKE_PREFIX_PATH="D:/Program_flies/qt_creat/APP/5.14.2/mingw73_64"
cmake --build build --target report report_tests
```
Expected: FAIL —— 报错找不到 `report/reporters.h`（include 头、`ReportRegistry::instance()` 未定义）。

- [ ] **Step 3: 写最小实现（接口头 + 注册表 + generate 桩）**

`include/report/reporters.h`：

```cpp
// report/include/report/reporters.h —— 报告接口（契约 §4.5，冻结）
//
// 本文件逐字实现契约 §4.5 的三个公开类型：
//   ReportData / IReporter / ReportRegistry
//
// 相对契约文本的「编译必要增补」（任务明示许可，不改冻结公开签名）：
//   1. 补齐 QtCore include（QDateTime/QList/QString/QStringList/QHash）；
//   2. ReportRegistry 补私有工厂表存储 + 私有默认构造（单例必需）。
#pragma once
#include <core_model/Chromatogram.h>
#include <core_processing/interfaces.h>   // 复用 QuantEntry/Peak
#include <QtCore/qdatetime.h>
#include <QtCore/qhash.h>
#include <QtCore/qlist.h>
#include <QtCore/qstring.h>
#include <QtCore/qstringlist.h>
namespace cdsw {

struct ReportData {                // = ReportColumns 的 Qt 版（见逆向 MODULE_06）
    QString sampleName;
    QString methodName;
    QDateTime acquiredAt;
    QList<Peak> peaks;
    QList<QuantEntry> quantEntries;
};

class IReporter {                 // = chromatogramReportSupplier
public:
    virtual ~IReporter() = default;
    virtual QString formatName() const = 0;        // "CSV" / "PDF"
    virtual QString fileSuffix() const = 0;        // ".csv"
    virtual bool generate(const ReportData& data, const QString& filePath) = 0;
};

class ReportRegistry {
public:
    static ReportRegistry& instance();
    void registerReporter(IReporter* (*factory)());
    QStringList availableFormats() const;
    IReporter* reporterFor(const QString& format) const;
private:
    ReportRegistry() = default;
    QHash<QString, IReporter* (*)()> m_reporterFactories;
};

} // namespace cdsw
```

`src/ReporterCsv.h`：

```cpp
// report/src/ReporterCsv.h —— CSV 报告器（IReporter 实现，模块内私有）
#pragma once
#include <report/reporters.h>

namespace cdsw {

class ReporterCsv : public IReporter {
public:
    QString formatName() const override;
    QString fileSuffix() const override;
    bool generate(const ReportData& data, const QString& filePath) override;
};

} // namespace cdsw
```

`src/ReporterCsv.cpp`（本任务 = 桩）：

```cpp
// report/src/ReporterCsv.cpp —— CSV 报告器（Task 2 实现真实写入，本任务先桩）
#include "ReporterCsv.h"

namespace cdsw {

QString ReporterCsv::formatName() const { return QStringLiteral("CSV"); }

QString ReporterCsv::fileSuffix() const { return QStringLiteral(".csv"); }

bool ReporterCsv::generate(const ReportData&, const QString&)
{
    return false;   // 桩：Task 2 实现
}

} // namespace cdsw
```

`src/ReportRegistry.cpp`：

```cpp
// report/src/ReportRegistry.cpp —— 报告注册表单例 + 内置格式注册（开闭原则）
#include <report/reporters.h>

#include <memory>

#include "ReporterCsv.h"

namespace cdsw {

namespace {

// 内置报告格式注册（模块初始化 = ReportRegistry::instance() 首次调用时执行一次）
bool registerBuiltIns(ReportRegistry& r)
{
    r.registerReporter([]() -> IReporter* { return new ReporterCsv; });
    return true;
}

} // namespace

ReportRegistry& ReportRegistry::instance()
{
    static ReportRegistry registry;
    static const bool registered = registerBuiltIns(registry);
    (void)registered;
    return registry;
}

void ReportRegistry::registerReporter(IReporter* (*factory)())
{
    if (!factory)
        return;
    // 注册需要键：实例化探针取 formatName（构造无副作用）
    std::unique_ptr<IReporter> probe(factory());
    if (probe)
        m_reporterFactories.insert(probe->formatName(), factory);
}

QStringList ReportRegistry::availableFormats() const
{
    QStringList formats = m_reporterFactories.keys();
    formats.sort();
    return formats;
}

IReporter* ReportRegistry::reporterFor(const QString& format) const
{
    const auto it = m_reporterFactories.constFind(format);
    return (it != m_reporterFactories.constEnd()) ? (it.value())() : nullptr;
}

} // namespace cdsw
```

`report/CMakeLists.txt`：

```cmake
# report —— 报告（契约 §4.5，依赖 core_processing 的 QuantEntry/Peak）
cdsw_add_module(report core_model core_processing)

# 金样数据：测试运行目录不定，把 golden 目录编译期注入（绝对源码路径）
if(CDSW_BUILD_TESTS)
    target_compile_definitions(report_tests PRIVATE
        REPORT_GOLDEN_DIR="${CMAKE_CURRENT_SOURCE_DIR}/tests/data")
endif()
```

- [ ] **Step 4: 构建 + 跑测试，确认绿**

```bash
cmake --build build --target report report_tests
ctest --test-dir build --output-on-failure -R report
```
Expected: PASS（registryDispatch 1 用例通过，其余 QSKIP）。

- [ ] **Step 5: 提交**

```bash
git add project/chromatography_workstation/report/
git commit -m "feat(report): 契约接口头 + ReportRegistry 注册表 + ReporterCsv 骨架
- reporters.h 逐字实现契约 §4.5（ReportData/IReporter/ReportRegistry）
- ReportRegistry 沿用 core_processing 工厂表模式，内置注册 CSV
- 测试接线：REPORT_GOLDEN_DIR 宏 + .gitattributes 强制 LF"
```

---

### Task 2: ReporterCsv 真实 CSV 生成 + 金样/边界/引号测试（TDD 红→绿）

**Files:**
- Create: `project/chromatography_workstation/report/tests/data/golden_report.csv`
- Modify: `project/chromatography_workstation/report/src/ReporterCsv.cpp`（桩 → 真实实现）
- Modify: `project/chromatography_workstation/report/tests/t_report.cpp`（QSKIP 桩 → 完整用例）

**Interfaces:**
- Consumes: Task 1 的 `ReporterCsv`、`ReportRegistry`、宏 `REPORT_GOLDEN_DIR`。
- Produces: 确定性 CSV 字节输出（分区式三节，格式见下方金样）；`generate()` 失败语义。

- [ ] **Step 1: 写金样参考文本（手写推导，LF）**

`tests/data/golden_report.csv`（逐字节 = 预期输出，行尾 LF，末尾一个换行）：

```
Sample Name,Benzene Calibration Std
Method Name,External Standard
Acquired At,2026-08-18T10:30:00
Number of Peaks,2
Number of Quantitation Entries,2

Peak Number,Apex RT (min),Start RT (min),Stop RT (min),Height,Area
1,1.250000,1.000000,1.500000,52000.000000,104000.000000
2,3.400000,3.100000,3.700000,31000.000000,77500.000000

Component,Apex RT (min),Area,Concentration,Unit
Benzene,1.250000,104000.000000,12.500000,mg/L
Toluene,3.400000,77500.000000,25.000000,mg/L
```

> 手算核对：75000ms=1.25min、60000ms=1.00min、90000ms=1.50min、204000ms=3.40min、186000ms=3.10min、222000ms=3.70min；峰乱序传入 → 输出按 apexRTMs 升序 = Benzene 先（#1）、Toluene 后（#2）。

- [ ] **Step 2: 写失败测试（替换 QSKIP 桩为真实用例）**

`tests/t_report.cpp`：删除 5 个 QSKIP 桩，替换为下面 5 个完整用例：

```cpp
void ReportTest::goldenReportMatchesReference()
{
    QTemporaryDir dir;
    QVERIFY(dir.isValid());
    const QString outPath = dir.filePath(QStringLiteral("report.csv"));

    std::unique_ptr<IReporter> reporter(ReportRegistry::instance().reporterFor(QStringLiteral("CSV")));
    QVERIFY(reporter);
    QVERIFY(reporter->generate(makeGoldenData(), outPath));

    const QString goldenPath = QStringLiteral(REPORT_GOLDEN_DIR "/golden_report.csv");
    QVERIFY2(QFileInfo::exists(goldenPath), qPrintable(goldenPath));
    QCOMPARE(readFile(outPath), readFile(goldenPath));
}

void ReportTest::emptyDataBoundary()
{
    QTemporaryDir dir;
    QVERIFY(dir.isValid());
    const QString outPath = dir.filePath(QStringLiteral("empty.csv"));

    std::unique_ptr<IReporter> reporter(ReportRegistry::instance().reporterFor(QStringLiteral("CSV")));
    QVERIFY(reporter);
    QVERIFY(reporter->generate(ReportData(), outPath));

    const QString expected = QStringLiteral(
        "Sample Name,\n"
        "Method Name,\n"
        "Acquired At,\n"
        "Number of Peaks,0\n"
        "Number of Quantitation Entries,0\n"
        "\n"
        "Peak Number,Apex RT (min),Start RT (min),Stop RT (min),Height,Area\n"
        "\n"
        "Component,Apex RT (min),Area,Concentration,Unit\n");
    QCOMPARE(QString::fromUtf8(readFile(outPath)), expected);
}

void ReportTest::peaksWithoutQuant()
{
    QTemporaryDir dir;
    QVERIFY(dir.isValid());
    const QString outPath = dir.filePath(QStringLiteral("peaks_only.csv"));

    ReportData d;
    d.sampleName = QStringLiteral("Sample With Peaks");
    d.acquiredAt = QDateTime(QDate(2026, 8, 18), QTime(9, 0, 0));
    d.peaks = { Peak{ 60000, 75000, 90000, 52000.0, 104000.0 } };

    std::unique_ptr<IReporter> reporter(ReportRegistry::instance().reporterFor(QStringLiteral("CSV")));
    QVERIFY(reporter);
    QVERIFY(reporter->generate(d, outPath));

    const QString expected = QStringLiteral(
        "Sample Name,Sample With Peaks\n"
        "Method Name,\n"
        "Acquired At,2026-08-18T09:00:00\n"
        "Number of Peaks,1\n"
        "Number of Quantitation Entries,0\n"
        "\n"
        "Peak Number,Apex RT (min),Start RT (min),Stop RT (min),Height,Area\n"
        "1,1.250000,1.000000,1.500000,52000.000000,104000.000000\n"
        "\n"
        "Component,Apex RT (min),Area,Concentration,Unit\n");
    QCOMPARE(QString::fromUtf8(readFile(outPath)), expected);
}

void ReportTest::rfc4180Quoting()
{
    QTemporaryDir dir;
    QVERIFY(dir.isValid());
    const QString outPath = dir.filePath(QStringLiteral("quoting.csv"));

    ReportData d;
    d.sampleName = QStringLiteral("A,B");                 // 含逗号 → 加引号
    d.methodName = QStringLiteral("Say \"hi\"");          // 含双引号 → 内部翻倍
    d.peaks = { Peak{ 60000, 60000, 60000, 1.0, 2.0 } };
    d.quantEntries = { QuantEntry{ 60000, QStringLiteral("line1\nline2"), 2.0, 0.0, QString() } }; // 含换行 → 加引号

    std::unique_ptr<IReporter> reporter(ReportRegistry::instance().reporterFor(QStringLiteral("CSV")));
    QVERIFY(reporter);
    QVERIFY(reporter->generate(d, outPath));

    const QString expected = QStringLiteral(
        "Sample Name,\"A,B\"\n"
        "Method Name,\"Say \"\"hi\"\"\"\n"
        "Acquired At,\n"
        "Number of Peaks,1\n"
        "Number of Quantitation Entries,1\n"
        "\n"
        "Peak Number,Apex RT (min),Start RT (min),Stop RT (min),Height,Area\n"
        "1,1.000000,1.000000,1.000000,1.000000,2.000000\n"
        "\n"
        "Component,Apex RT (min),Area,Concentration,Unit\n"
        "\"line1\nline2\",1.000000,2.000000,0.000000,\n");
    QCOMPARE(QString::fromUtf8(readFile(outPath)), expected);
}

void ReportTest::generateFailsOnBadPath()
{
    QTemporaryDir dir;
    QVERIFY(dir.isValid());
    // 父目录不存在 → QFile::open(WriteOnly) 失败 → generate 返回 false
    const QString badPath = dir.filePath(QStringLiteral("missing/report.csv"));

    std::unique_ptr<IReporter> reporter(ReportRegistry::instance().reporterFor(QStringLiteral("CSV")));
    QVERIFY(reporter);
    QCOMPARE(reporter->generate(ReportData(), badPath), false);
}
```

- [ ] **Step 3: 跑测试，确认失败**

```bash
cmake --build build --target report report_tests
ctest --test-dir build --output-on-failure -R report
```
Expected: FAIL —— `goldenReportMatchesReference` 等用例 `QVERIFY(generate(...))` 失败（generate 桩返回 false）。

- [ ] **Step 4: 实现真实 CSV 写入**

`src/ReporterCsv.cpp` 全文替换为：

```cpp
// report/src/ReporterCsv.cpp —— CSV 报告器实现（分区式：表头节 + 峰表 + 定量表）
#include "ReporterCsv.h"

#include <QtCore/qfile.h>
#include <QtCore/qtextstream.h>

#include <algorithm>

namespace cdsw {

namespace {

// RFC4180 引号：字段含逗号/双引号/换行时加双引号包裹，内部双引号翻倍
QString csvEscape(const QString& field)
{
    const bool needQuote = field.contains(QLatin1Char(','))
        || field.contains(QLatin1Char('"'))
        || field.contains(QLatin1Char('\n'));
    if (!needQuote)
        return field;
    QString escaped = field;
    escaped.replace(QLatin1String("\""), QLatin1String("\"\""));
    return QLatin1Char('"') + escaped + QLatin1Char('"');
}

// 保留时间毫秒 → 分钟定点 6 位（MODULE_06 MINUTE_CORRELATION_FACTOR = 60000.0）
QString rtMinutes(qint64 ms)
{
    return QString::number(ms / 60000.0, 'f', 6);
}

QString fixed6(double v)
{
    return QString::number(v, 'f', 6);
}

void writeCsvRow(QTextStream& out, const QList<QString>& cells)
{
    for (int i = 0; i < cells.size(); ++i) {
        if (i > 0)
            out << QLatin1Char(',');
        out << csvEscape(cells.at(i));
    }
    out << QLatin1Char('\n');
}

} // namespace

QString ReporterCsv::formatName() const { return QStringLiteral("CSV"); }

QString ReporterCsv::fileSuffix() const { return QStringLiteral(".csv"); }

bool ReporterCsv::generate(const ReportData& data, const QString& filePath)
{
    QFile file(filePath);
    // 不带 QIODevice::Text：\n 原样写（LF 行尾），保证跨平台字节一致
    if (!file.open(QIODevice::WriteOnly))
        return false;

    QTextStream out(&file);   // QTextStream 默认 UTF-8

    // —— 表头节 ——
    writeCsvRow(out, { QStringLiteral("Sample Name"), data.sampleName });
    writeCsvRow(out, { QStringLiteral("Method Name"), data.methodName });
    writeCsvRow(out, { QStringLiteral("Acquired At"), data.acquiredAt.toString(Qt::ISODate) });
    writeCsvRow(out, { QStringLiteral("Number of Peaks"), QString::number(data.peaks.size()) });
    writeCsvRow(out, { QStringLiteral("Number of Quantitation Entries"),
                       QString::number(data.quantEntries.size()) });
    out << QLatin1Char('\n');

    // —— 峰表（按 apexRTMs 升序排序后编号 1..N；MODULE_06 PeakRetentionTimeComparator ASC）——
    writeCsvRow(out, { QStringLiteral("Peak Number"), QStringLiteral("Apex RT (min)"),
                       QStringLiteral("Start RT (min)"), QStringLiteral("Stop RT (min)"),
                       QStringLiteral("Height"), QStringLiteral("Area") });
    QList<Peak> sorted = data.peaks;
    std::stable_sort(sorted.begin(), sorted.end(),
                     [](const Peak& a, const Peak& b) { return a.apexRTMs < b.apexRTMs; });
    for (int i = 0; i < sorted.size(); ++i) {
        const Peak& p = sorted.at(i);
        writeCsvRow(out, { QString::number(i + 1), rtMinutes(p.apexRTMs), rtMinutes(p.startRTMs),
                           rtMinutes(p.stopRTMs), fixed6(p.peakHeight), fixed6(p.peakArea) });
    }
    out << QLatin1Char('\n');

    // —— 定量表（保持 ReportData 给定顺序）——
    writeCsvRow(out, { QStringLiteral("Component"), QStringLiteral("Apex RT (min)"),
                       QStringLiteral("Area"), QStringLiteral("Concentration"),
                       QStringLiteral("Unit") });
    for (const QuantEntry& e : data.quantEntries) {
        writeCsvRow(out, { e.componentName, rtMinutes(e.apexRTMs), fixed6(e.area),
                           fixed6(e.concentration), e.unit });
    }

    out.flush();
    return file.error() == QFileDevice::NoError;
}

} // namespace cdsw
```

- [ ] **Step 5: 跑测试，确认全绿**

```bash
cmake --build build --target report report_tests
ctest --test-dir build --output-on-failure -R report
```
Expected: PASS（6 用例全绿，无 QSKIP）。

- [ ] **Step 6: 提交**

```bash
git add project/chromatography_workstation/report/
git commit -m "feat(report): ReporterCsv 分区式 CSV 生成 + 金样逐字节比对测试
- CSV 三节：表头元数据 + 峰表（按 apexRTMs 升序编号）+ 定量表
- RFC4180 引号、LF 行尾、UTF-8、'f',6 定点、RT 分钟制 → 跨平台字节一致
- 金样 golden_report.csv 手写推导，测试逐字节比对；含空峰/无定量/引号/失败路径边界"
```

---

### Task 3: 全量验证 + 文档收尾（PROJECT.md + 记忆）

**Files:**
- Modify: `PROJECT.md`（§2 report 行：状态 → 待合并）
- Create: `docs/memory/report.md`

**Interfaces:**
- Consumes: Task 1–2 交付的 report 模块。
- Produces: 可合回 main 的 dev-report 分支 + 文档。

- [ ] **Step 1: 全量软件审查（D1/D2/D3）**

```bash
export PATH="/d/Program_flies/qt_creat/APP/5.14.2/mingw73_64/bin:/d/Program_flies/qt_creat/APP/Tools/mingw730_64/bin:$PATH"
cmake --build build          # D1 全量编译（含主程序壳 + 全模块）
ctest --test-dir build --output-on-failure -R report   # D2 本模块全绿
ctest --test-dir build -N | grep -i report             # D3 确认 add_test 注册
```
Expected: 全量编译通过；report 测试全绿；`report_tests` 出现在 `ctest -N` 里。

- [ ] **Step 2: 接口纪律自查（D4）**

```bash
grep -rE "QtWidgets|QtGui|QtQml" project/chromatography_workstation/report/  # 应为空
grep -rE '#include "\.\./' project/chromatography_workstation/report/        # 应为空（无越界 include）
grep -rE "#include <(core_model|core_processing)/" project/chromatography_workstation/report/  # 只允许 include/<模块>/
```
Expected: 无 QtWidgets；无相对越界 include；只引两模块接口头。

- [ ] **Step 3: 更新 PROJECT.md §2 report 行**

`PROJECT.md` 第 34 行 `| report | dev-report | worktrees/report/ | 待开工 | ... |` 改为：

```
| report | dev-report | worktrees/report/ | ✅ 待合并 | M5 完成（契约 §4.5 接口 + ReporterCsv + ReportRegistry + 金样测试全绿，2026-08-18，ctest report 全绿） | MODULE_06 | 无 |
```

同时把 `§1` 里程碑总览第 22 行 M5 的状态列 `⏳ 待开工` 改为 `🔨 代码完成测试全绿，待合并`。

- [ ] **Step 4: 写记忆文件 `docs/memory/report.md`**

```markdown
# report 模块记忆

## 2026-08-18 report 线程 — M5 报告生成器完成（待合并）
- **做了什么**：实现契约 §4.5 的 report 模块，ctest 全绿（6 用例：注册表分发/金样逐字节比对/空峰边界/有峰无定量/RFC4180 引号/失败路径）。文件：`report/include/report/reporters.h`（逐字契约）、`report/src/ReporterCsv.{h,cpp}`、`report/src/ReportRegistry.cpp`、`report/tests/t_report.cpp` + `tests/data/golden_report.csv`。
- **为什么这么设计**：CSV 分区式布局（表头节+峰表+定量表）用户拍板；LF 行尾 + UTF-8 + `'f',6` 定点 + RT 分钟制保证金样逐字节跨平台一致；`ReportRegistry` 沿用 core_processing Registry 的工厂表+探针取键模式（开闭原则：加格式=加实现类+注册一行）；金样手写推导避免循环验证；`.gitattributes` 强制 LF 防 autocrlf 检出转 CRLF。
- **下一步**：等主控审查后把 dev-report 合回 main（M1/M2 已合，report 属并行四路之一）。
- **卡点/依赖**：① 契约头缺 `<QtCore/qdatetime.h>`（QDateTime 值成员需完整类型），按 core_processing 先例补「编译必要增补」注释，不改冻结签名。② 金样测试要读 `tests/data/` 下的文件，cdsw_add_module 不拷数据文件 → 用 `target_compile_definitions(REPORT_GOLDEN_DIR=绝对源码路径)` 注入。③ Windows 跑 ctest 前必须 `export PATH` 加 Qt/MinGW bin，否则 0xc0000135。
```

- [ ] **Step 5: 提交 + 汇报**

```bash
git add PROJECT.md docs/memory/report.md
git commit -m "doc(report): M5 完成 —— PROJECT.md §2 状态更新 + report 模块记忆"
git status   # 确认工作区干净
```

汇报：改了哪些文件、ctest 结果、有无卡点。**不自行 merge main**，等主控合并。

---

## Self-Review 记录

- **Spec 覆盖**：设计文档 §3 全部决策逐项落到 Task（布局→Task2、行尾/编码→ReporterCsv 实现、排序→峰表 stable_sort、空边界→emptyDataBoundary、引号→rfc4180Quoting、注册表→Task1、错误处理→generateFailsOnBadPath、所有权→registryDispatch unique_ptr）；验收 §5 六条 → 六个用例；DoD D1–D4/D6 → Task3。
- **占位符扫描**：无 TBD/TODO；Task2 Step4 给出完整实现代码，无「相似于 Task1」式简写。
- **类型一致性**：`ReportRegistry::instance()/registerReporter/availableFormats/reporterFor`、`IReporter::{formatName,fileSuffix,generate}`、宏 `REPORT_GOLDEN_DIR`、`Peak{...}/QuantEntry{...}` 聚合初始化在各 Task 中签名一致；`QString::number(x,'f',6)`、`std::stable_sort` 用法统一。
