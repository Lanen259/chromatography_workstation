# 06 — Quantification（定量）

> **当前状态：🔶 框架版（源码未就位）**
> 目标：逆向定量模块：calibration / external standard / internal standard / normalization / concentration calculation / response factor / statistics。
> 结论须追溯源码；无法确认处标「待验证」。

---

## 1. 本 Phase 要逆向的主题清单

| # | 主题 | 必答问题 |
|---|---|---|
| Q1 | Calibration | 标定流程：标样→曲线→未知样定量 |
| Q2 | External standard | 外标法实现 |
| Q3 | Internal standard | 内标法实现（内标物/内标峰） |
| Q4 | Normalization | 面积归一化/百分比计算 |
| Q5 | Concentration calculation | 由峰面积/峰高到浓度的换算 |
| Q6 | Response factor | 响应因子（校准曲线斜率等） |
| Q7 | Statistics | 线性回归、R²、SD/RSD、置信区间 |

---

## 2. 背景假设（⚠️ 待验证，仅作线索）

| # | 假设 | 说明 | 状态 |
|---|---|---|---|
| QU1 | 校准曲线默认线性（y=ax+b，最小二乘），可能支持加权 | 待确认 ⚠️ | ⚠️ |
| QU2 | 定量依赖峰面积（或峰高）作为响应值 | 关联 05 ⚠️ | ⚠️ |
| QU3 | 外标/内标是「定量方法」配置，可切换 | 待确认 ⚠️ | ⚠️ |
| QU4 | 统计量（R²/RSD 等）独立于校准计算，可能复用第三方统计库 | 待确认 ⚠️ | ⚠️ |
| QU5 | 定量配置与处理方法(Method)一起保存 | 关联 04 ⚠️ | ⚠️ |

---

## 3. 定量登记表（🔲 待填充）

| 环节 | 实现类 | 文件 | 入口方法 | 输入 | 输出 | 关键参数 | 状态 |
|---|---|---|---|---|---|---|---|
| 校准曲线拟合 | | | | | | | 🔲 |
| 外标计算 | | | | | | | 🔲 |
| 内标计算 | | | | | | | 🔲 |
| 归一化 | | | | | | | 🔲 |
| 浓度换算 | | | | | | | 🔲 |
| 响应因子 | | | | | | | 🔲 |
| 统计量 | | | | | | | 🔲 |

---

## 4. 待确认问题清单（❓）

| # | 问题 |
|---|---|
| Q1Q | 校准曲线支持哪些模型（线性/多项式/对数/幂）？ |
| Q2Q | 外标法是否需要独立的标定表对象？存在哪？ |
| Q3Q | 内标法：内标峰如何指定（保留时间窗口？名称？） |
| Q4Q | 归一化基于面积还是峰高？百分比精度？ |
| Q5Q | 浓度单位/稀释因子是否进入计算？ |
| Q6Q | 响应因子是否可手工输入覆盖计算值？ |
| Q7Q | 统计量在哪里计算、写入哪个对象（Result？） |

---

## 5. 输出要求（回填后必须给出）

1. 定量全流程调用链（峰面积 → 定量结果写入）。
2. 校准/外标/内标/归一化的算法公式与代码出处对照表。
3. 定量结果在数据模型（03 M8 Result）中的落点字段。
4. 与 05（面积/峰高）输入来源的交叉引用。
