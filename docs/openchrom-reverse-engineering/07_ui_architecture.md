# 07 — UI Architecture（UI 架构）

> **当前状态：🔶 框架版（源码未就位）**
> 目标：逆向主要 UI：chromatogram view / peak table / method editor / result view / data browser / dialogs。
> 重点回答：**UI 如何读取分析结果？UI 如何修改参数？参数修改如何重新触发计算？**

---

## 1. UI 组件清单（本 Phase 必查）

| # | 组件 | 预期作用（待确认） |
|---|---|---|
| U1 | Chromatogram View | 色谱曲线显示（缩放/平移/选区） |
| U2 | Peak Table | 峰表（保留时间、面积、高度、鉴定、定量列） |
| U3 | Method Editor | 处理方法编辑（各阶段参数） |
| U4 | Result View | 定量/鉴定结果汇总 |
| U5 | Data Browser / Navigator | 数据文件树 / 工作区浏览 |
| U6 | Dialogs | 打开文件、参数设置、向导等 |

---

## 2. 背景假设（⚠️ 待验证，仅作线索）

| # | 假设 | 说明 | 状态 |
|---|---|---|---|
| UI1 | 基于 Eclipse SWT/JFace（ViewPart / EditorPart / Dialog） | RCP 惯例 ⚠️ | ⚠️ |
| UI2 | 曲线绘制可能用 SWT 自绘或第三方图表库（如 JFreeChart） | 待确认 ⚠️ | ⚠️ |
| UI3 | UI 通过 listener 监听模型变化来刷新 | 关联 03 D2 ⚠️ | ⚠️ |
| UI4 | 处理在**后台线程**执行，UI 用 Job/进度反馈 | 待确认 ⚠️ | ⚠️ |
| UI5 | 参数修改 → 保存到方法对象 → 重新执行处理管线 | 关联 04 ⚠️ | ⚠️ |

---

## 3. 三问追踪表（🔲 待填充）

### 3.1 UI 如何读取分析结果？

| 组件 | 读取的模型对象 | 读取类/方法 | 刷新机制 | 状态 |
|---|---|---|---|---|
| Chromatogram View | | | | 🔲 |
| Peak Table | | | | 🔲 |
| Result View | | | | 🔲 |

### 3.2 UI 如何修改参数？

| 组件 | 可编辑参数 | 编辑控件 | 写入对象/方法 | 状态 |
|---|---|---|---|---|
| Method Editor | | | | 🔲 |
| Peak Table（手工修改） | | | | 🔲 |

### 3.3 参数修改如何重新触发计算？

```text
参数变更
  → 事件/监听触发
  → 更新方法对象 / 模型标记 dirty
  → 重新调度处理（Job / 手动"重新处理"命令？）
  → 结果写回 → UI 刷新
```

| 环节 | 实现类/方法 | 文件 | 状态 |
|---|---|---|---|
| 参数变更事件 | | | 🔲 |
| 方法对象更新 | | | 🔲 |
| 重新计算调度 | | | 🔲 |
| UI 刷新通知 | | | 🔲 |

---

## 4. 待确认问题清单（❓）

| # | 问题 |
|---|---|
| U1Q | 曲线绘制的技术选型（自绘/库）？性能策略（抽稀/降采样）？ |
| U2Q | 峰表是 SWT Table 还是第三方表格？编辑方式？ |
| U3Q | 方法编辑是向导(Page-based)还是对话框/页签？ |
| U4Q | Result View 数据来自模型还是重算？ |
| U5Q | 数据浏览器的文件加载与 01 S6 打开命令关系？ |
| U6Q | UI 线程阻塞策略：处理期间 UI 是否冻结？ |

---

## 5. 输出要求（回填后必须给出）

1. UI 组件 ↔ 模型对象 的读取映射表。
2. 参数修改→重算→刷新的**完整调用链**（sequence 图）。
3. UI 与引擎解耦程度评估（界面能否脱离引擎独立测试）。
4. 与 02（数据流）、03（模型）、04/05（处理）的交叉引用。
