# 编码风格规范（精简公约）

> 从 gas_chromatograph 现有 Qt/C++ 代码风格提炼，**修改任何代码前先读本文件**。
> 三条核心目标：**可扩展性**（新功能低成本接入、不破坏分层）与**可读性**（一眼看懂意图）与**适应多并行开发，接口保留规范**。

## 1. 命名

- 类/结构体/枚举类型：`PascalCase`（`DetectorPage`、`ModbusRegisterDescriptor`）。
- 函数/方法/信号/槽：`camelCase`（`setDetectorSnapshot`、`buildModbusReadPlan`）。
- 变量（局部/成员/参数）：`snake_case`（`registerCount`）；成员变量加 `m_` 前缀（`m_detectorWidgets`）。
- 全局变量加 `g_` 前缀；文件内私有辅助用 `static` 限定作用域。
- 枚举用 `enum class` 强类型；禁止魔法数字，常量集中命名。

## 2. 头文件

- 头文件保护用 `#pragma once`（MinGW + CMake AUTOMOC 完全支持；全项目统一，与契约 §4 一致）。
- include 顺序：本头 → 工程头 → Qt 头 → 标准库头。
- 尽量前置声明（`class QLineEdit;`），降低耦合。

## 3. 类型与数据

- 数据描述/DTO 用 `struct` + 成员默认初始化（`int registerCount = 1;`）。
- 结构体自带校验/查询方法（`isValid()`、`containsAddress()`）。
- 协议域用命名空间组织（`namespace Protocol {}`）。
- 讲究 `const`：`const` 方法、`const` 引用参数。

## 4. 分层架构（可扩展性的根本）

- **UI Page**：只做控件、输入、信号、显示刷新；禁止直接依赖底层传输/协议。
- **Service**：业务数据与协议转换、轮询组、快照发布；不建线程/socket。
- **传输层**：只做队列、组帧、CRC、收发；不了解业务。
- **协议层**：只放静态地址/descriptor/纯函数；无运行时状态。
- 页面刷新统一 `Page::setXxxSnapshot(...)`；业务变化只改快照内容，UI 不变。

## 5. 实现细节

- 早返回守卫（guard clause），减少嵌套。
- `for (int i = 0; i < n; ++i)` 配 `.at(i)`；STL 算法用 `constBegin()/constEnd()`。
- 字符串常量用 `QStringLiteral()`。
- 中文注释只写"为什么"，不写"是什么"；行尾不残留空格。
- 长函数按单一职责拆分；同类功能保持同一写法，便于对照复用。

## 6. UI 线程红线

- UI 线程禁止长阻塞、死循环、同步网络/串口/大文件 I/O。
- 耗时操作放 worker/异步任务，避免卡界面。

## 7. 修改纪律

- 最小改动、接口稳定、沿用现有分层；禁止大重构或绕过 Service 直连传输层。
- 新增功能按已有模块范本照抄链路，先说明差异的必要性再动手。

## 8. UI 设计规范（Qt Designer / `.ui` 优先）

- **可复用的页面一律用 `.ui` 文件设计**（Qt Designer 可视化）：凡"页面 / 面板 / 对话框"（Page / Panel / Dialog），先建 `.ui` → 自动生成 `ui_xxx.h` → 类内 `setupUi()` 装配；**禁止纯代码手写布局 / 控件摆放**。
- `.ui` 与代码分工：`.ui` 管"长什么样"（布局、控件、样式），C++ 只管"干什么"（信号槽、数据绑定、业务逻辑）。禁止在代码里 `setGeometry()`、禁止手动 `new QXxx` 拼布局。
- 纯代码 UI 只允许两种例外：
  1. 自定义绘制控件（继承 `QWidget` 重写 `paintEvent` 的曲线 / 仪表等）—— `.ui` 表达不了绘制内容；
  2. 运行时动态生成、无复用价值的临时元素。
- 自定义绘制控件也必须以**提升类（promote）**占位进 `.ui`，不能因此整页纯代码拼装。
- 复用粒度：一个 `.ui` = 一个可复用页面；公共控件抽成独立 `.ui`（子页面）再组合。
- 理由：可视化设计可复用、可预览、可交接，契合"界面由 ui 模块统一交付、多 agent 并行"的协作方式。
