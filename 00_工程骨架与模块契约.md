# 色谱工作站 · Qt/C++ 工程骨架与模块契约（多 Agent 并行开发主控文档）

> **读者**：后续所有参与本工程开发的 AI agent / 开发者。
> **用途**：本文件是本工程的「主控 + 契约」。它定义了模块边界、接口签名（= OpenChrom 的扩展点规格）、CMake 骨架、测试策略与多 agent 并行工作流。
> **铁律**：任何 agent 开工前**先完整读取本文件**；只实现自己模块契约内的东西；**不得修改其他模块的契约**（接口头文件由契约文档固定，改契约需先在此文档登记并通知所有人）。
> **目标形态**：嵌入式色谱工作站（CDS）。可分离的功能全部分离成独立 CMake 库 + 独立测试，多 agent 并行开发，最终合并到主分支即可运行。

---

## 1. 设计思想（OpenChrom → 本工程）

本工程参照 OpenChrom 的模块化思想（逆向文档见 `E:\My_project\QT\openchrom\docs\reverse-engineering\`），但**砍掉 OSGi 运行时与动态插件**，换用更轻的「接口契约 + 编译期静态库」：

| OpenChrom 机制 | 本工程对应 | 说明 |
|---|---|---|
| OSGi bundle（插件） | **CMake 库 target（模块）** | 每个模块 = 一个库 + 自带测试，独立编译、独立测试 |
| 扩展点 `plugin.xml` | **接口头文件 + 注册表** | 契约只写接口签名，各模块实现之 |
| ChemClipse 核心平台（模型/引擎/扩展点定义） | **`core_model` + `core_processing`** | 核心层，纯 QtCore，禁止依赖 UI/硬件 |
| `chromatogramSupplier` 转换器扩展点 | **`io` 模块的 `IChromatogramImporter/Exporter`** | 文件格式各自一个实现类 |
| `peakDetectorSupplier` / `peakIntegratorSupplier` 等 | **`core_processing` 的算法接口 + 注册表** | 每种算法 = 一个类，按 id 注册 |
| Feature（打包/独立更新单元） | CMake target 分组 | 运行时可选加载 |
| `IChromatogramSelection.fireUpdateChange` → listener | **Qt signals/slots** | 模型只广播变化，UI 订阅 |
| 后台 Job 线程 | **QThread / QtConcurrent + 信号回传** | UI 不阻塞 |
| CSD/MSD/WSD 检测器域 | **`Signal` 统一域**（单检测器 FID/UV 起步） | 后续可按域扩展命名空间 |

**核心结论：OpenChrom 让全球开发者并行开发 68 个插件互不冲突，靠的是「先定死插头规格（扩展点接口），再让各供应商各自实现」。本工程复制这一机制——接口契约先行，agent 各自实现。**

---

## 2. 总体架构与依赖方向（不可违反）

```
┌─────────────────────────────────────────────────────────────┐
│ app/            主程序壳（生成可运行 exe）——唯一"组装清单"      │
│   MainWindow 组装各视图；只管装配，不含业务逻辑                 │
├─────────────────────────────────────────────────────────────┤
│ ui/             界面层（Qt Widgets，唯一允许依赖 GUI 的模块）    │
│   曲线视图 / 峰表 / 方法编辑 / Selection 桥接                   │
├───────────────┬──────────────────────┬───────────────────────┤
│ io/           │ report/              │ acq/                  │
│ 导入导出转换器 │ 报告生成器           │ 采集(实时/环形缓冲/HAL) │
│ 依赖 core_model│ 依赖 core_model      │ 依赖 core_model(仅)    │
├───────────────┴──────────────────────┴───────────────────────┤
│ core_processing/   处理引擎（纯 QtCore）                       │
│   滤波/基线/峰检测/积分/定量 接口 + 注册表 + 管线执行器           │
├─────────────────────────────────────────────────────────────┤
│ core_model/        领域模型（纯 QtCore，零依赖，最底层）         │
│   Signal / Chromatogram / Peak / Method / Selection            │
└─────────────────────────────────────────────────────────────┘
```

**依赖铁律（违反 = 契约破坏，集成时必失败）：**
1. 依赖方向只允许 `ui → core / acq / io / report`，`acq → core_model`，`io/report → core_model`，`core_processing → core_model`。
2. **`core_model` 和 `core_processing` 禁止 include `<QtWidgets>` / `<QtGui>` / `<QtQml>`**，只准 `<QtCore>`。违反此条，模块就不可独立测试。
3. **`acq` 禁止依赖 `ui` 和 `core_processing`**（采集是实时路径，不经过分析管线；保证独立硬件联调）。
4. `core_model` 不得依赖任何模块（最底层，先合并，全项目地基）。
5. 模块间只通过**接口头文件**互相认识，不通过具体类。集成就靠接口注入 + signals/slots。

---

## 3. 模块清单（每个模块 = 一个 agent 的独立任务单元）

| # | 模块 | 目录 | 依赖 | 对应 OpenChrom | 产出物 |
|---|---|---|---|---|---|
| 1 | core_model | `core_model/` | 无（QtCore） | `org.eclipse.chemclipse.model`（IChromatogram/IPeak/IPeakModel） | 纯数据类 + 元数据 |
| 2 | core_processing | `core_processing/` | core_model | ChemClipse processing + filter/peak.detector/integrator/quantitation | 算法接口 + 实现 + 注册表 + 管线 |
| 3 | acq | `acq/` | core_model | （OpenChrom 社区版无采集，自研） | HAL 接口 + 环形缓冲 + 采集控制器 + 驱动 |
| 4 | io | `io/` | core_model | `*.converter.supplier.*`（cdf/csv/gaml…） | 导入导出转换器 + 注册表 |
| 5 | report | `report/` | core_model | `chromatogramReportSupplier` + csv/excel/pdf 报告器 | 报告数据 + IReporter + 各格式实现 |
| 6 | ui | `ui/` | core_model + 上述模块接口 | ChemClipse UX 视图 + SWTChart | 曲线/峰表/方法编辑视图 + 主窗口 |
| 7 | app | `app/` | 全部 | `*.product` 产品定义 | 可运行 exe + 装配代码 |

**模块内目录约定**（每个模块统一）：
```
core_processing/
  include/core_processing/    # 对外接口头文件（契约本体，其他模块只准 include 这里）
  src/                        # 实现
  tests/                      # 独立测试（QTest，CTest 注册）
  CMakeLists.txt
```

---

## 4. 契约（各 Agent 唯一必须遵守的接口签名）

> 所有接口都放在 `include/<module>/` 下。**契约 = 签名 + 语义注释，禁止 agent 改签名**。实现类放在各自 `src/` 私有即可，或注册到注册表。
> 命名：类前缀 `I` 表示接口（interface）；信号统一用 `sig*` 前缀；单位**保留时间一律毫秒（qint64）**，强度用 `double`（相对强度 0–100）。

### 4.1 core_model —— 领域模型（地基，第一个做）

```cpp
// core_model/include/core_model/Signal.h
#pragma once
#include <QtCore/qglobal.h>
namespace cdsw {

// 一个采样点：保留时间(ms) + 强度。CSD 单通道起步。
struct Signal {
    qint64 retentionTimeMs = 0;   // 保留时间，毫秒制（OpenChrom 约定）
    double intensity = 0.0;       // 信号强度（可 0–100 相对或原始 AD 值）
};

// 色谱图：有序信号序列 + 元数据。纯数据容器，不含任何分析逻辑。
class Chromatogram {
public:
    QString name() const;                 // 文件名/样品名
    void setSignalPoints(const QVector<Signal>&);
    const QVector<Signal>& signalPoints() const;  // 原始信号
    int scanCount() const;
    qint64 startTimeMs() const; qint64 stopTimeMs() const;
    double minIntensity() const; double maxIntensity() const;
    // 标记是否已处理（脏标记，供 UI 决定是否重跑管线）
    bool isDirty() const; void setDirty(bool);
private:
    QString m_name;
    QVector<Signal> m_signal;
    bool m_dirty = true;
};

// 峰三要素：start/apex/stop（毫秒）。OpenChrom IPeakModel 语义。
struct Peak {
    qint64 startRTMs = 0; qint64 apexRTMs = 0; qint64 stopRTMs = 0;
    double peakHeight = 0.0;
    double peakArea = 0.0;          // 由积分器填充
    QVector<Signal> profile;        // 峰内原始点（可空）
};

// 处理方法 = 有序步骤列表（OpenChrom "processing method" 的 Qt 版）
struct ProcessingStep {
    QString id;                     // 对应注册表里的算法 id，如 "sg_smooth" / "first_derivative_peak_detector"
    QVariantMap parameters;         // 算法参数
};
struct Method { QVector<ProcessingStep> steps; };

// 选择（OpenChrom IChromatogramSelection 语义 → 触发 UI 刷新的广播载体）
class Selection : public QObject {
    Q_OBJECT
public:
    void setRange(qint64 startMs, qint64 stopMs);
    qint64 startMs() const; qint64 stopMs() const;
    void setPeak(const Peak&); const Peak& peak() const;
signals:
    void sigSelectionChanged();     // = fireUpdateChange；UI/其他模块订阅
private:
    qint64 m_startMs = 0; qint64 m_stopMs = 0;
    Peak m_peak;
};

} // namespace cdsw
```

> 测试点：结构体字段读写、Chromatogram 元数据/范围计算、Selection 信号发出。

### 4.2 core_processing —— 处理引擎（接口 + 注册表 + 管线）

```cpp
// core_processing/include/core_processing/interfaces.h
#pragma once
#include <core_model/Chromatogram.h>
#include <core_model/Peak.h>
#include <QtCore/qlist.h>
namespace cdsw {

// —— 算法接口（= OpenChrom 各 supplier 扩展点）——
class IFilter {                 // 滤波/平滑（对应 filter.supplier.savitzkygolay 等）
public:
    virtual ~IFilter() = default;
    virtual QString id() const = 0;          // 唯一 id，注册表键
    virtual QString displayName() const = 0;
    virtual void apply(Chromatogram& chrom) = 0;   // 原地修改信号
};

class IBaselineDetector {       // 基线检测（对应 xxd.baseline.detector）
public:
    virtual ~IBaselineDetector() = default;
    virtual QString id() const = 0;
    virtual QVector<Signal> detect(const Chromatogram& chrom) const = 0; // 返回基线点
};

class IRawPeak {
public:
    virtual ~IRawPeak() = default;
    virtual qint64 startRTMs() const = 0;
    virtual qint64 apexRTMs() const = 0;
    virtual qint64 stopRTMs() const = 0;
};

class IPeakDetector {           // 峰检测（对应 firstderivative / amdis）
public:
    virtual ~IPeakDetector() = default;
    virtual QString id() const = 0;
    virtual QList<IRawPeak*> detect(const Chromatogram& chrom) const = 0;
    // 实现者返回堆对象，由调用方释放；或用 shared_ptr 约定（见下方别名）
};

class IIntegrator {             // 峰积分（对应 integrator.supplier.trapezoid）
public:
    virtual ~IIntegrator() = default;
    virtual QString id() const = 0;
    virtual void integrate(Chromatogram& chrom, QList<Peak>& peaks) const = 0;
};

class IQuantifier {             // 定量（对应 peakQuantifierSupplier + 校准曲线）
public:
    virtual ~IQuantifier() = default;
    virtual QString id() const = 0;
    // 输入峰面积表 + 校准表，输出定量结果行
    virtual QList<QuantEntry> quantitate(const QList<Peak>& peaks,
                                         const CalibrationTable& calib) const = 0;
};

// —— 数据载体（供 Quantifier 使用）——
struct QuantEntry {
    qint64 apexRTMs = 0;
    QString componentName;      // 组分名（对照表匹配）
    double area = 0.0;
    double concentration = 0.0; // 计算浓度
    QString unit;
};
struct CalibrationPoint { double concentration; double area; };
struct CalibrationTable { QString componentName; QVector<CalibrationPoint> points; };

// —— 注册表（= 扩展点注册机制。算法实现类在 src 里，模块初始化时注册）——
class Registry {
public:
    static Registry& instance();
    void registerFilter(IFilter* (*factory)());
    void registerPeakDetector(IPeakDetector* (*factory)());
    void registerIntegrator(IIntegrator* (*factory)());
    void registerBaseline(IBaselineDetector* (*factory)());
    void registerQuantifier(IQuantifier* (*factory)());
    IFilter* createFilter(const QString& id) const;
    IPeakDetector* createPeakDetector(const QString& id) const;
    IIntegrator* createIntegrator(const QString& id) const;
    QStringList availableFilterIds() const;
    QStringList availablePeakDetectorIds() const;
    // ... 其余访问器
private:
    Registry() = default;
};

// —— 管线执行器（= processing method 引擎：按 Method.steps 顺序执行）——
class ProcessingPipeline {
public:
    explicit ProcessingPipeline(const Registry& registry);
    void execute(const Method& method, Chromatogram& chrom);
    // 每步执行后 emit，供 UI 刷新/进度
signals:
    void sigStepFinished(int stepIndex, const QString& id);
    void sigFinished();
};

} // namespace cdsw
```

> **内置算法实现（各自独立测试，可并行开发）**：
> - `FilterSavitzkyGolay`（SG 平滑，对应 ChemClipse `filter.supplier.savitzkygolay`）
> - `BaselineLinear`（线性基线，对应一阶导数峰检测配套的基线处理）
> - `PeakDetectorFirstDerivative`（**一阶导数峰检测**——本工程核心算法，源码证据见逆向文档 MODULE_04 / `PeakDetectorCSD.java`）
> - `IntegratorTrapezoid`（梯形积分，对应 `integrator.supplier.trapezoid`）
> - `QuantifierCalibration`（内标/外标校准曲线）
>
> 每个算法一个 CMake target 的粒度过大，放同一模块 `src/` 即可，但**每个算法必须自带测试**（见 §6）。

### 4.3 acq —— 采集（实时路径，零 UI 依赖，HAL 可 mock）

```cpp
// acq/include/acq/hal.h —— 硬件抽象（模拟/真机都可实现，测试用 MockDevice）
#pragma once
#include <core_model/Chromatogram.h>
#include <QtCore/qvector.h>
namespace cdsw {

struct DeviceConfig {
    int channelCount = 1;
    double sampleRateHz = 0.0;    // 采样率
    int adcRange = 0;             // AD 量程
    // ... 各设备扩展字段
};

class IDevice {                   // 硬件接口（= 你的"仪器驱动扩展点"）
public:
    virtual ~IDevice() = default;
    virtual bool open(const DeviceConfig&) = 0;
    virtual void close() = 0;
    virtual bool startAcquisition() = 0;
    virtual bool stopAcquisition() = 0;
    virtual qint64 read(double* buffer, qint64 maxSamples) = 0; // 返回实际读到的样本数
    virtual QString deviceInfo() const = 0;
};

// 环形缓冲：采集线程写入、消费线程读取。独立可测。
class RingBuffer {
public:
    explicit RingBuffer(int capacity);
    bool write(const double* data, int count);   // 满则丢/覆盖策略（可配置）
    int read(double* out, int maxCount);          // 非阻塞
    int available() const; int capacity() const;
};

// 采集控制器：持有一个 IDevice，后台线程拉取 → 环形缓冲 → 发信号。
class AcquisitionController : public QObject {
    Q_OBJECT
public:
    AcquisitionController(IDevice* device, QObject* parent = nullptr);
    void start(); void stop();
    // 向模型/UI 推送的一段信号（采样点已带时间戳由外部换算）
    qint64 samplesRead() const;
signals:
    void sigDataAcquired(const QVector<double>& samples);  // 原始 ADC 序列
    void sigDeviceError(const QString& message);
    void sigStarted(); void sigStopped();
};

} // namespace cdsw
```

> 嵌入式要点：`RingBuffer` 无锁/原子实现；`AcquisitionController` 的线程只依赖 `IDevice`，**测试用 `MockDevice` 注入假采样序列**，不需要真硬件。

### 4.4 io —— 导入导出转换器

```cpp
// io/include/io/converters.h
#pragma once
#include <core_model/Chromatogram.h>
namespace cdsw {

struct ImportResult {
    bool ok = false;
    QString errorMessage;
};

class IChromatogramImporter {     // = chromatogramSupplier importConverter
public:
    virtual ~IChromatogramImporter() = default;
    virtual QStringList supportedExtensions() const = 0; // {".csv", ".cdf"}
    virtual QString formatName() const = 0;
    virtual ImportResult import(const QString& filePath, Chromatogram& out) = 0;
};

class IChromatogramExporter {     // = chromatogramSupplier exportConverter
public:
    virtual ~IChromatogramExporter() = default;
    virtual QStringList supportedExtensions() const = 0;
    virtual bool export_(const QString& filePath, const Chromatogram& chrom) = 0;
};

// 扩展名 → 转换器 的注册表（文件打开对话框按扩展名枚举，= OpenChrom UI 枚举 suppliers）
class ConverterRegistry {
public:
    static ConverterRegistry& instance();
    void registerImporter(IChromatogramImporter* (*factory)());
    void registerExporter(IChromatogramExporter* (*factory)());
    IChromatogramImporter* importerFor(const QString& filePath) const;
    IChromatogramExporter* exporterFor(const QString& filePath) const;
    QStringList allImportExtensions() const;
};

} // namespace cdsw
```

> 起步实现：`ImporterCsv`（CSV 导入）、`ExporterCsv`。后续照 `net.openchrom.*.converter.supplier.*` 的写法加 CDF/GAML 等，一个格式一个类。

### 4.5 report —— 报告生成器

```cpp
// report/include/report/reporters.h
#pragma once
#include <core_model/Chromatogram.h>
#include <core_processing/interfaces.h>   // 复用 QuantEntry/Peak
namespace cdsw {

struct ReportData {                // = ReportColumns 的 Qt 版（见逆向 MODULE_06）
    QString sampleName;
    QString methodName;
    QDateTime acquiredAt;
    QList<Peak> peaks;
    QList<QuantEntry> quantEntries;
};

class IReporter {                 // = chromatogramReportSupplier
public:
    virtual ~IReporter() = default;
    virtual QString formatName() const = 0;        // "CSV" / "PDF"
    virtual QString fileSuffix() const = 0;        // ".csv"
    virtual bool generate(const ReportData& data, const QString& filePath) = 0;
};

class ReportRegistry {
public:
    static ReportRegistry& instance();
    void registerReporter(IReporter* (*factory)());
    QStringList availableFormats() const;
    IReporter* reporterFor(const QString& format) const;
};

} // namespace cdsw
```

### 4.6 ui —— 界面层（唯一可依赖 QtWidgets 的模块）

```cpp
// ui/include/ui/  —— 视图组件，全部通过 core 接口读数据、不直接碰设备
// ui/include/ui/ChromatogramView.h   曲线控件：setChromatogram(Chromatogram*)；缩放/平移/选区
// ui/include/ui/PeakTableView.h       峰表：QTableView + 适配 Selection/Peak 的 QAbstractTableModel
// ui/include/ui/MethodEditorView.h    方法编辑：列出 Method.steps，增删改参数 → 写回 Method
// ui/include/ui/MainWindow.h          装配：菜单/工具栏/左右分栏；持有 Selection 与管线

// Selection 桥接（= OpenChrom fireUpdateChange 的 Qt 版，UI 与引擎唯一握手点）：
//   曲线选区 → Selection.sigSelectionChanged → 触发管线重跑 → 峰表/结果刷新
class SelectionController : public QObject {
    Q_OBJECT
public:
    SelectionController(Selection* selection, ProcessingPipeline* pipeline, QObject* parent = nullptr);
    void onChromatogramChanged();   // 数据更新 → setDirty → 重新 execute(Method, chrom)
signals:
    void sigPeaksUpdated(const QList<Peak>&);
};
```

> ui 模块契约：**不直接 new 具体算法/设备类**，全部经 `Registry::instance()` 与 `ConverterRegistry` 取；`core_model/core_processing` 出现任何 include 该 ui 头文件即为契约破坏。

---

## 5. CMake 骨架（每模块独立编译 + 独立测试）

```
chromatography_workstation/
  CMakeLists.txt                  # 根：cmake_minimum_required / project / add_subdirectory 各模块
  cmake/cdsw_module.cmake         # 公共函数 cdsw_add_module(<name> <deps>...) —— 生成库+测试约定
  core_model/      CMakeLists.txt  src/  include/  tests/
  core_processing/ CMakeLists.txt  src/  include/  tests/
  acq/             CMakeLists.txt  src/  include/  tests/
  io/              CMakeLists.txt  src/  include/  tests/
  report/          CMakeLists.txt  src/  include/  tests/
  ui/              CMakeLists.txt  src/  include/  tests/
  app/             CMakeLists.txt  src/              # 只链接不测试（组装）
  build/            # git 忽略，不入库
```

**关键约定（保证"独立编译 + 独立测试"）：**
1. 每个模块是 **STATIC 库**，`target_include_directories(核心的 include/ 目录)`，只暴露接口头。
2. 每个模块 `tests/` 用 **Qt Test（QTest）** + `enable_testing()` / `add_test()`；模块可**脱离主程序单独 `ctest` 跑通**——这是"独立测试验证"的硬标准。
3. `cdsw_add_module(name 依赖...)` 统一：加头文件路径、禁 UI 模块对 core 的反向可见性、注册测试。
4. 根 CMakeLists **不写死勾选项**：`option(CDSW_BUILD_TESTS ON)`，`option(CDSW_BUILD_UI ON)`（嵌入式目标可关 UI 单独编 core 三件套）。
5. Qt：`find_package(Qt6 COMPONENTS Core Widgets Test)`（无 Qt6 时降 Qt5）。

**示例（模块 CMakeLists 统一写法）：**
```cmake
# core_processing/CMakeLists.txt
cdsw_add_module(core_processing core_model)   # 依赖 core_model
target_link_libraries(core_processing PRIVATE Qt6::Core)
```

**构建/测试命令（每模块 agent 在自己 worktree 里跑）：**
```bash
cmake -S . -B build -DCDSW_BUILD_TESTS=ON
cmake --build build --target core_processing
ctest --test-dir build --output-on-failure -R core_processing
```

---

## 6. 测试策略（独立测试验证 = 本工程硬性要求）

| 模块 | 测试手段 | 关键用例 | 无需依赖 |
|---|---|---|---|
| core_model | QTest 纯数据 | 范围计算 / 元数据 / Selection 信号 | 无 |
| core_processing | QTest + **金标准信号** | 喂写死的 Signal 波形 → 断言峰数/峰面积/基线点（算法正确性基准，OpenChrom MODULE_04 一阶导数算法全链可移植） | 无硬件/UI |
| acq | QTest + **MockDevice** | 环形缓冲读写/覆盖、控制器启停、mock 喂采样 → 断言 sigDataAcquired 内容 | 无真硬件 |
| io | QTest 回环 | CSV 导出→导入→逐点比对；损坏文件报错 | 无 |
| report | QTest 金样对比 | 固定 ReportData → 生成 CSV → 与 golden 文本比对 | 无 |
| ui | QTest widget 冒烟 | 视图可创建/加载数据/选区触发管线（无头环境 `QT_QPA_PLATFORM=offscreen`） | 无真设备 |

**Mock 约定（嵌入式联调关键）：** `acq/tests/MockDevice.h` 实现 `IDevice`，按配置吐出正弦/阶梯采样序列；所有依赖 `IDevice` 的测试一律注入 mock，保证 CI 无硬件可跑。

---

## 7. 多 Agent 并行工作流（本项目最终目标形态）

### 7.1 并行前提：先合并地基
`core_model` **必须先合并进主分支**，因为所有模块依赖它的头文件。地基层未就位前，其他 agent 可先按 §4 契约**自建空壳头文件桩编译**，但不提交。

### 7.2 一模块一分支 + worktree 并行开发
```bash
# 主分支先合并 core_model 后，为每个模块开分支（本工程的 worktree 均在仓库内 worktrees/ 下，已预建，见 §11.2）
git worktree add worktrees/core_processing -b dev-core-processing   # agent A：core_processing
git worktree add worktrees/acq             -b dev-acq               # agent B：acq
git worktree add worktrees/io              -b dev-io                # agent C：io
git worktree add worktrees/report          -b dev-report            # agent D：report
git worktree add worktrees/ui              -b dev-ui                # agent E：ui（可最后/在接口稳定后）
```
- 每个 worktree = 独立目录 + 独立 `build/`，`ctest` 只跑自己的模块，互不阻塞。
- **Windows 上 worktree 正常可用**；Qt Creator 把每个 worktree 目录当作独立工程打开。
- 分支命名建议：`dev-<模块名>`，feature 粒度按模块，不再往下拆。

### 7.3 契约冻结规则（防并行冲突的根）
1. 接口签名在 §4 冻结；agent 只允许**实现**接口，不允许改签名。
2. 需要新增算法/格式时：新增接口实现类 + 注册表注册，**不动既有类**（开闭原则）。
3. 确需改契约：先在本文档 §4 登记变更 + 版本号，其他 agent 合并前先同步。

### 7.4 合并顺序（依赖方向决定）
```
main: core_model → core_processing → acq → io → report → ui → app(集成)
```
每步合并要求：该模块 `ctest` 全绿 + 不影响已合并模块。`ui` 和 `app` 最后合并，做端到端冒烟。

### 7.5 集成验证
主分支最终：
```bash
cmake -S . -B build -DCDSW_BUILD_TESTS=ON
cmake --build build
ctest --test-dir build --output-on-failure    # 全模块测试一次性跑通 = 集成通过
```

---

## 8. 嵌入式注意事项

1. **实时性隔离**：采集线程（`acq`）是实时路径，与分析管线（`core_processing`）完全解耦；数据经环形缓冲→模型→（可选）分析，任何分析慢都不阻塞采集。
2. **HAL 隔离硬件**：`IDevice` 使驱动可插拔。真机驱动（串口/USB/网口）是 `IDevice` 的一个实现，与逻辑分离，先 mock 联调、后接硬件。
3. **UI 可替换**：嵌入式若显示端是远程/无头，`ui` 模块可整体不编译（`CDSW_BUILD_UI=OFF`），采集/核心在设备端独立运行，UI 通过 IPC/网络另行接入。
4. **禁止 GUI 依赖泄漏**：`core_model / core_processing / acq / io / report` 五个模块头文件里出现 `#include <QtWidgets>` 一律视为契约破坏（CI 可用 grep 规则拦截）。

---

## 9. 开发顺序里程碑

| 里程碑 | 内容 | 并行 agent 数 | 验收 |
|---|---|---|---|
| M0 | 本契约文档 + 根 CMake 骨架 + `cdsw_module.cmake` + 目录结构 | 1 | 空工程可 cmake 配置成功 |
| M1 | **core_model**（含测试）合并 main | 1 | 自身 ctest 绿 |
| M2 | core_processing（接口+注册表+管线+算法+测试） | 1–2 | 金标准信号测试绿 |
| M3 | acq（HAL+环形缓冲+控制器+mock） | 1 | mock 测试绿 |
| M4 | io（CSV 转换器+回环测试） | 1 | 回环测试绿 |
| M5 | report（CSV 报告器+金样测试） | 1 | 金样测试绿 |
| M6 | ui（视图+Selection 桥接+冒烟） | 1–2 | offscreen 冒烟绿 |
| M7 | app 集成 + 端到端 | 1 | 全模块 ctest 绿 + 可运行 exe |

> 每个里程碑可独立交付、独立验收；**本工程的最终形态就是"主分支一个空壳 + 无数可独立开发的模块"，与 OpenChrom "一个壳 + 无数插件" 同构，但去掉了 OSGi 运行时。**

---

## 10. 与其他文档的关系

- 设计依据（逆向知识库）：`E:\My_project\QT\openchrom\docs\reverse-engineering\`（主控 `README.md`；模块系列 `module/MODULE_00..06`；`11_workstation_composition.md` 给出 10 大功能模块→组件映射与采集缺口）。
- 本工程根目录后续应有：`00_工程骨架与模块契约.md`（本文件）+ 各模块契约子文档（可选，按需拆分）。
- 铁律继承自逆向项目：结论须可溯源；本文件是**前向规格**，接口签名若与实际实现冲突，以本文件为准并回改实现。

---

## 11. Git 环境与 worktree 框架（已搭建 2026-08-16）

### 11.1 现状（本机事实）
- 主仓库位置：`E:\My_project\QT\chromatography_workstation`（**本文件夹 = main 分支的工作目录**）。
- 已 `git init -b main`，默认分支 **main**。
- 已创建 `.gitignore`（忽略 `build/`、`worktrees/`、Qt Creator 杂项）。
- 已提交初始提交：契约文档 + `.gitignore` + 现有 `docs/`。
- **尚未添加远程仓库**：后续自行 `git remote add origin <url>`，不影响本地框架。

### 11.2 worktree 布局（所有 worktree 位于仓库内 `worktrees/` 子目录下）

| 分支 | worktree 目录 | 用途 | 状态 |
|---|---|---|---|
| `main` | `./`（本文件夹） | 主分支，只放已合并的稳定内容 | 已建 |
| `dev-core-model` | `worktrees/core_model/` | 领域模型（地基，先合并） | 已建 |
| `dev-core-processing` | `worktrees/core_processing/` | 处理引擎 | 已建 |
| `dev-acq` | `worktrees/acq/` | 采集（HAL + 环形缓冲） | 已建 |
| `dev-io` | `worktrees/io/` | 导入导出转换器 | 已建 |
| `dev-report` | `worktrees/report/` | 报告生成器 | 已建 |
| `dev-ui` | `worktrees/ui/` | 界面层 | 已建 |
| `dev-app` | `worktrees/app/` | 组装（最后合并） | 已建 |

> ⚠️ **`worktrees/` 已被 `.gitignore` 忽略**：这些目录只是"把分支拿出来干活的位置"，不是仓库内容。分支本体存在 `git branch` 里（几字节的指针），删目录不影响分支。
> ⚠️ 不要在 worktree 目录里再次 `git init`（它们已是主仓库的链接 worktree）。

### 11.3 后续开发固定动作

1. **进某个模块干活**：直接打开对应 worktree 目录（如 `worktrees/acq/`），它就是该分支的完整工作副本；编译/测试在它**自己**的 `build/` 下做，不影响其他分支。
2. **查看现状**：`git worktree list` / `git branch -a`
3. **新增模块分支**（已预建，一般不用）：`git worktree add worktrees/<name> -b dev-<name>`
4. **删除某 worktree（分支保留）**：`git worktree remove worktrees/<name>`
5. **合并回 main（每模块验证通过后，按 §7.4 顺序）**：
   ```bash
   # ① 在自己分支里先把 main 拉进来，跑全量测试（防"自己绿、合起来炸"）
   cd worktrees/<name>
   git merge main
   ctest --test-dir build --output-on-failure
   # ② 全绿才合并回 main
   cd ../..
   git checkout main && git merge dev-<name>
   ```
6. **推送 / 远程仓库（后续你自行创建后）**：
   ```bash
   # 主目录：首次关联并推送 main
   git remote add origin <url>
   git push -u origin main
   # 各 worktree 内推送自己的分支
   git push -u origin dev-<name>
   ```

### 11.4 铁律补充（叠加第 2 节依赖铁律之上）
- **不要**把 `worktrees/`、`build/` 提交进仓库（已 gitignore；若误 `git add`，用 `git reset <path>` 撤销）。
- **不要**在 worktree 里 `git init` 新仓库。
- 合并顺序固定：`core_model → core_processing → acq → io → report → ui → app`；每一层在 main 上验证全绿后，才放行下一层。
- 每分支只改自己模块的 `src/`、`include/`、`tests/`、`CMakeLists.txt`；改契约先在本文件 §4 登记。

